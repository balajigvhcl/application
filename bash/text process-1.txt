# sed
sed -i edit files directly without saving seperately 
's/source/dest/g' - search and replace 

sed -n 's/world/word/p' file.txt
print the output 

sed -r 's/(word|line/p)' file.txt

-r adding script from a file, to execute sed commands.
[root@localhost bash]# sed -f script.sed file.txt
hello bash
liene1 liner
.kine
line2
[root@local

-l 10 '1' appends $ at end of each line
 sed 's/^/hostname: /' file.txt
hostname: hello world
hostname: liene1 liner
hostname: .kine
hostname: line1

=------------------------
cat /etc/redhat-release: Reads the file content, which typically looks like "Red Hat Enterprise Linux release 9.4 (Plow)".
match($0, /[0-9]/): Finds the position of the first numerical digit in the line.substr(..., 1): 
Extracts exactly one character starting from that position—effectively giving you just the major version number