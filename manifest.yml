version: '3.8'

services:
  httpd:
    image: httpd:latest
    container_name: httpd-server
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/local/apache2/htdocs
    restart: unless-stopped
